import type { AnalysisReport } from "@/lib/analyzer/types";
export function ScoreCard({ report }: { report: AnalysisReport }) {
  return <section className="surface rounded-2xl p-6"><div className="flex items-center justify-between"><p className="text-sm text-slate-500">امتیاز پیشنهادی</p><span className="rounded-full bg-teal-50 px-3 py-1 text-xs font-medium text-teal-700">WCAG 2.2</span></div><div className="mt-3 flex items-end gap-3"><span className="text-7xl font-black text-teal-700">{report.score}</span><span className="mb-3 text-slate-500">از ۱۰۰</span></div><p className="mt-3 text-lg font-bold text-slate-900">{report.scoreLabel}</p><div className="mt-5 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-teal-600" style={{ width: `${report.score}%` }} /></div><p className="mt-5 text-sm leading-7 text-slate-500">این امتیاز برای اولویت‌بندی اصلاحات است و جایگزین ارزیابی انسانی کامل WCAG نمی‌شود.</p></section>;
}
