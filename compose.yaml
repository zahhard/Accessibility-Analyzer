services:
  app:
    build:
      context: .
    image: accessibility-analyzer:latest
    restart: unless-stopped
    init: true
    shm_size: 1gb
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
